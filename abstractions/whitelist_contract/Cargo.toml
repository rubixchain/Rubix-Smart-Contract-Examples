[package]
name = "whitelist_contract"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]


[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
wasm_macros = { path = "../wasm_macros" }
wasm_lib = { path = "../wasm_lib" }